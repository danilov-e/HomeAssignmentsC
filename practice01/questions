1) У нас есть заголовок stats.hpp, в котором определяется переменная типа int g_requests, но этот заголовок включается в 2 cpp, а каждый cpp является отдельной единицей трансляции,
и выходит, что у нас в программе два определения переменной с внешним связыванием, и из-за этого линкер не знает, какое выбрать.

3) Такое определение не выдаёт ошибки, потому что мы используем глобальную константу, которая связывается внутренним связыванием. Получается, у каждой единицы трансляции своя копия
или версия этого объекта, которая хранится в разных местах в памяти. Но это плохо, потому что ломает логику и  занимает слишком много места.  
